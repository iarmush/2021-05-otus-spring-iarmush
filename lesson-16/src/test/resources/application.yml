spring:
  data:
    mongodb:
      port: 0
      database: test

  mongock:
    runner-type: ApplicationRunner
    change-logs-scan-package:
      - ru.otus.lesson.changelogs

  shell:
    interactive:
      enabled: false